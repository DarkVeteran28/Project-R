def ask_document(
    hotel_id: str,
    conversation_id: str,
    question: str,
) -> str:
    """
    Temporary RAG interface.

    The actual document retrieval system will be
    implemented in the next phase.
    """

    return "The document search system is not connected yet."